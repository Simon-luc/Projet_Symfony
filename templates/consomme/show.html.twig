{% extends 'base.html.twig' %}

{% block title %}Consomme{% endblock %}

{% block body %}
    <h1>Consomme</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ consomme.id }}</td>
            </tr>
            <tr>
                <th>Date_consommation</th>
                <td>{{ consomme.dateConsommation ? consomme.dateConsommation|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Quantite_consomme</th>
                <td>{{ consomme.quantiteConsomme }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_consomme_index') }}">back to list</a>

    <a href="{{ path('app_consomme_edit', {'id': consomme.id}) }}">edit</a>

    {{ include('consomme/_delete_form.html.twig') }}
{% endblock %}
